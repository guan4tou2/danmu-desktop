# 設計稿 10 · 補充畫面（收尾）

來源：`10 補充畫面.dc.html`。全部沿用 03 共用規範元件，**無新元件**。

## G1 · Admin 首次導覽 `#/onboarding-tour`（3 步氣泡）

- 全頁遮罩 `rgba(15,23,42,.35)`；目標區用 `box-shadow: 0 0 0 4px #fff, 0 0 0 9999px rgba(15,23,42,.35)` 打洞
- 氣泡卡 360px、圓角 16px、上方 16px 旋轉 45° 小三角
- 內容：「1 / 3」12px 主色 → 標題 17px 700 → 說明 14px → 底列「略過導覽」（次要文字）＋「下一步」主按鈕 36px
- 底部一行進度文字：`1 顯示層 · 2 觀眾怎麼進來 · 3 遇到不當內容`
- 第 1 步文案：**先讓大螢幕連上** —「在放簡報的那台電腦安裝桌面端，輸入這個網址，這張卡就會變綠。之後開關都在這裡。」

## G2 · 場次詳情 `#/session-detail`

- 頁首：`‹ 紀錄與匯出` 返回連結 → 場次名 22px → 時間 13px 次要 → 右側「重播」（次）＋「匯出」（主）
- KPI 卡：4 欄 grid、`tabular-nums`。訊息 1,204／觀眾 312／每分鐘高峰 86 (09:47)／被擋下 23
- 搜尋列「⌕ 搜尋這場的訊息」＋ 分段控制「全部 / 被擋下 23」
- 訊息表：48px 列＝時間戳(60px, tabular)／顏色點／內文／暱稱
  - 被擋下的列：底 `rgba(220,38,38,.04)`、內文紅色刪除線、右側標「封鎖字「加line」」

## D4 · 顯示層啟動動畫 — **保留原 LINK START**

設計稿明確保留 `overlay-ws.js` 現有實作原樣：Orbitron 700 青色 `#00d4ff` glitch 字（紅／藍位移雙層）＋ `linkstart.png` 3D 隧道（perspective 5px、6s lsMove、兩組相位差 3s）。

- 時長：**3 秒後 1.5 秒淡出，共 4.5 秒**，預設開啟
- 切換位置：桌面端「設定 › 一般 › 啟動動畫」→ LINK START／領域展開／自訂文字／關閉

## S7 · 桌面端「更新已下載」

- **顯示中時不打斷；關閉顯示層後才提示**
- 320px 對話框：app icon 48px → 「Danmu Fire 5.4.0 已準備好」17px 600 → 「重新啟動即完成更新，約需 10 秒。設定與伺服器位址都會保留。」13px → 直排兩顆：「重新啟動並更新」（主）／「下次關閉時再更新」（文字鈕）

## V2 · 觀眾頁 emoji 列

- 位置：輸入框與動作列之間，**鍵盤上方一列**，常用優先
- 6 格：🔥 👏 ❤️ 😂 ✨ ＋「更多」；每格 44×44、圓角 12px、24px emoji
- 輸入框聚焦樣式：1px 主色框 ＋ `0 0 0 3px rgba(2,132,199,.15)` 光暈；右側字數 `8/80` tabular

## V3 · 暱稱編輯（樣式抽層內的第二層）

- Sheet 標題列三欄：`‹ 樣式`（返回）／「暱稱」（置中 17px 700）／「完成」
- 72px 預覽條，即時顯示「阿明 你的彈幕會長這樣」
- 輸入框 52px、字數 `2/12`
- 說明：「會顯示在你每則彈幕前面。留空就用「匿名」。」
- 底部一列開關「顯示暱稱」

## K3 · Admin 深色對稱（審核頁為例）

**深色對映表**（程式端用 `light-dark()` 一組 token 完成，不需兩套樣式）：

| 角色 | 淺色 | 深色 |
|---|---|---|
| 底 | `#F4F6F9` | `#0B1120` |
| 卡片 | `#FFFFFF` | `#121A2B` |
| 浮起 | `#EEF2F6` | `#1A2437` |
| 分段選中 | `#FFFFFF` | `#334155` |
| 主色 | `#0284C7` | `#38BDF8`（其上文字 `#04121F`） |
| 成功 | `#15803D` | `#34D399` |
| 危險 | `#B91C1C` | `#F87171` |
| 次要文字 | `#5B6B7F` | `#94A3B8` |
| 髮線 | slate-900 @8% | slate-400 @12% |

側欄圖示上的通知點：`#34d399`，7px，定位 `top:6px; right:6px`。
